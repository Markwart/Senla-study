ћодуль осуществл€ет обработку (чтение/запись) файлов текстового формата csv.

¬ модуль включены три аннотации:

- @CsvEntity(filename=Фorders.csvФ, valuesSeparator=Ф;Ф, entityId=ФidФ) - ставитс€ над классом, объекты которого должны сохран€тьс€ в csv формат

- @CsvProperty(propertyType=ProperyType.SimpleProperty, columnNumber=5) - ставитс€ над атрибутом который должен сохран€тьс€ в csv формат

- @CsvProperty(propertyType=ProperyType.CompositeProperty, keyField=ФidФ) - ставитс€ над атрибутами объектного типа, которые должны быть сохранены как св€зи 

ќбработка файлов осуществл€етьс€ с помощью трех основных методов:
- writeToCsv() - запись данных (экземпл€ров классов со значени€ми полей) в файлы текстового формата csv
- readFromCsv() - чтение файлов тестового формата csv, поместив данные в строковом виде (String) в коллекцию
- parseToEntity()  - преобразование данных в виде строки в объекты с пол€ми и их значени€ми



